{
  "name": "@pos-pro/backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "scripts": {
    "dev": "convex dev",
    "dev:setup": "convex dev --configure --until-success",
    "check-types": "tsc -p convex/tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "convex": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@pos-pro/config": "workspace:*",
    "@types/node": "^24.3.0",
    "convex-test": "^0.0.53",
    "typescript": "catalog:",
    "vitest": "^4.1.9"
  }
}
